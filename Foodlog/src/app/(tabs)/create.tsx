// Placeholder route for the center "+" tab. The tab press is intercepted in the
// tabs layout to open the log modal, so this screen is never actually shown.
export default function CreateTab() {
  return null;
}
